export class Cajera {
    nombreCompleto: string;
    turno: string;
    numeroCaja: number;
  
    constructor(nombreCompleto: string, turno: string, numeroCaja: number) {
      this.nombreCompleto = nombreCompleto;
      this.turno = turno;
      this.numeroCaja = numeroCaja;
    }
  }